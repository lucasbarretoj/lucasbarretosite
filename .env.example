# A aplicação não exige variáveis de ambiente em runtime.
#
# O deploy usa GitHub Secrets, nunca arquivos .env:
# FTP_SERVER
# FTP_USERNAME
# FTP_PASSWORD

# URL pública do Web App do Google Apps Script. Em produção, use a URL /exec.
NEXT_PUBLIC_CONTACT_ENDPOINT=
